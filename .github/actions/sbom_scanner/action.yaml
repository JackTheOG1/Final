name: 'SBOM Generation and Scanning'
description: 'Generating an SBOM and scans for known vulnerabilities'

outputs:
  sbom-file: 
    value: ${{ steps.sbom-gen-and-scane.outputs.sbom-file }}

runs:
  using: 'docker'
  image: 'Dockerfile'
  
